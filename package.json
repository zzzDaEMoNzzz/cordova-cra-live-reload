{
  "name": "cordova-cra-live-reload",
  "version": "0.2.5",
  "description": "Live reload when developing on Cordova + CRA",
  "private": false,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist types",
    "build": "npm run clean && tsc",
    "test": "cordova-cra-live-reload run"
  },
  "main": "./dist/index.js",
  "bin": {
    "cordova-cra-live-reload": "./dist/bin/index.js"
  },
  "types": "./types/index.d.ts",
  "dependencies": {
    "wait-on": "^6.0.0"
  },
  "engines": {
    "node": ">=8.3.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zzzDaEMoNzzz/cordova-cra-live-reload"
  },
  "keywords": [
    "cordova",
    "create-react-app",
    "live-reload"
  ],
  "files": [
    "/dist",
    "/template",
    "/types"
  ],
  "devDependencies": {
    "@types/wait-on": "^5.3.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  }
}